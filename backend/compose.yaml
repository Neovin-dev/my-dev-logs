services:
  postgres:
    image: postgres:18
    container_name: devlogs-postgres

    environment:
      POSTGRES_USER: devlogs
      POSTGRES_PASSWORD: devlogs
      POSTGRES_DB: devlogs

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data:
